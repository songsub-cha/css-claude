"""Consistency guard for CSS domain-expert routing.

Keeps `agents/*.md` frontmatter, the executor Domain_Dispatch_Table, and the
README specialist tables in sync so a new domain specialist can never be defined
without also being dispatchable and documented.
"""
